spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: password
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true
server:
  port: 8082

